type Query {
    getRows(
        page:Int!
        limit:Int!
        ):Csv
}
type Csv {
    rows: [Row]
    total: Int
}
type Row {
    columns:[Column]
}
type Column {
    name: String
    value: String
}